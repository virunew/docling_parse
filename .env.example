# Docling JSON Parser Configuration
# Copy this file to .env and customize the values

# Path to the input PDF file
DOCLING_PDF_PATH=/path/to/your/document.pdf

# Directory for output files (default: "output")
DOCLING_OUTPUT_DIR=output

# Logging verbosity level (DEBUG, INFO, WARNING, ERROR, CRITICAL) (default: INFO)
DOCLING_LOG_LEVEL=INFO

# Optional path to additional configuration file
# DOCLING_CONFIG_FILE=/path/to/docling_config.yaml 